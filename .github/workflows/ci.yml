name: CI Pipeline

on:
  pull_request:
    branches:
      - phoenix-master-stream8

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '17'
          distribution: 'temurin

      - name: Run tests
        run: ./gradlew test