apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: user-service-namespace
spec:
  selector:
    app: minio
  ports:
    - name: api
      port: 9000
      targetPort: 9001
    - name: console
      port: 9001
      targetPort: 9001
  type: ClusterIP

