apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: user-service-namespace
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000
  type: NodePort
  