spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost

services:
  s3:
    endpoint: https://s3.storage.selcloud.ru
    accessKey: 12d79a21feca4323bd8146eb5a3b6405
    secretKey: 80de617d75f14947b76492b4b46be9aa
    region: ru-1
    bucket: alterality-s3

avatar_api:
  dice_bear:
    url: "https://api.dicebear.com/9.x/initials/svg?scale=50&backgroundType=gradientLinear&radius=50&seed="


server:
  port: 8080

logging:
  level:
    root: info

project-service:
  host: localhost
  port: 8082

payment-service:
  host: localhost
  port: 9080

application:
  goals:
    max-active: 3