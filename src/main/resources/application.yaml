app:
  leaderboard:
    max-cached-size: 1000
  avatar:
    apiUrl: "https://api.dicebear.com/5.x/avataaars"
    dicebearPngEndpoint: "/png?seed="
    localhostUrlPrefix: "http://localhost:9000"
    fileExtension: ".png"
    contentType: "image/png"
  minio:
    avatars:
      endpoint: http://localhost:9000
      access-key: HZykibzYtPG1ABN2J073
      secret-key: qjGfUbiitoZyWPNYS24G4EQKDsxCba9rK28DoqEj
    bucket:
      name: "corpbucket"

payment:
  api:
    url: http://localhost:9081/api/payment

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      auto-offset-reset: earliest
      max-poll-records: 100
      session-timeout-ms: 20000
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      topics:
        user-ban: "user-ban-topic"
      groups:
        user-ban: "user-ban-group"

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost

server:
  port: 8080

logging:
  level:
    root: info

project-service:
  host: localhost
  port: 8082

payment-service:
  host: localhost
  port: 9080
