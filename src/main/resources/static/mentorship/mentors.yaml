delete:
  tags:
    - MentorshipController
  summary: Remove mentor for the provided mentee by id
  operationId: deleteMentor
  parameters:
    - name: menteeId
      in: query
      description: mentee id
      required: true
      schema:
        $ref: 'resources.yaml#/components/parameters/id'
    - name: mentorId
      in: query
      description: mentor id
      required: true
      schema:
        $ref: 'resources.yaml#/components/parameters/id'
  responses:
    '200':
      description: Mentor successfully removed
    '400':
      description: Invalid params

